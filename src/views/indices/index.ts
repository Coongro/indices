/**
 * Índices — entry de la vista (lo referencia el coongro.manifest.json).
 * Regenerable; no agregar código acá — composición en `indices.view.ts`,
 * datos en `use-indices.ts`, lógica custom en `handlers.ts`.
 */
export { IndicesView } from './indices.view.js';
